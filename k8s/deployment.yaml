apiVersion: apps/v1
kind: Deployment
metadata:
  name: "afrojackman"
  namespace: "agents-ctp"
  labels:
    app: "afrojackman"
    team: "ctp"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "afrojackman"
  template:
    metadata:
      labels:
        app: "afrojackman"
        team: "ctp"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-afrojackman"
      containers:
        - name: "afrojackman"
          image: "acrshoppoc.azurecr.io/agents/afrojackman:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-afrojackman"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
