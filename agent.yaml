agent:
  id: "afrojackman"
  team: "ctp"
  runtime: aks
  interaction_model: "standalone"
  guardrail_profile: "strict"

identity:
  sp_client_id: "80a76f9b-067d-4e4f-9142-b4bc1ee03121"
  keyvault_url: "https://kv-shop-poc.vault.azure.net/"

tools: []

evaluation:
  suite: default-strict
  ci_gate: true
  pass_threshold: 0.95
  datasets:
    - id: golden-dataset
      version: v1.0
      source: repo
      path: evals/datasets/golden-dataset.json

output_policy:
  violation_threshold: 3
  window_minutes: 1
  action: block
  notify_webhook: 

prompts:
  active_version: v1.0
  source: prompts/system.txt
